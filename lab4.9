def bmi(w,h):
    x = (h/100)**2
    y = w/x
    print("ค่าดัชนีมวลกายคือ %.2f" % y)
    return y
    
def what(n):
    if n < 18.50 :
        print("นํ้าหนักน้อย/ผอม")
    elif n >=18.50 and n <=22.90 :
        print(">> ปกติสุขภาพดี <<")
    elif n >=23 and n <=24.90 :
        print(">> ท้วม/โรคอ้วนระดับ 1 <<")
    elif n >=25 and n <=29.90 :
        print(">> อ้วน/โรคอ้วนระดับ 2 <<")
    elif n >30 :
        print(">> อ้วนมาก/โรคอ้วนระดับ 3 อันตรายน้า <<")
    else : print("---ใส่นํ้าหนักส่วนสูงสลับรึป่าวว---")


w = float(input("นํ้าหนักตัว: "))
h = float(input("ส่วนสูง: "))
what(bmi(w,h))

